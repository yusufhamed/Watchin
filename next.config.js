/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['image.tmdb.org', 'assets.stickpng.com', 'eagle-sensors.com', 'www.userlogos.org'],
    },
}

module.exports = nextConfig